# Test-only defaults for PipelineBootstrap.loadConfig override-merge tests.
# These root keys back the `SampleConfig` reader in PipelineBootstrapSpec. PureConfig
# ignores the additional library reference.conf keys that ConfigSource.default also
# carries, so reading SampleConfig at the root resolves cleanly from these defaults.
api-key = "default-key"
max-items = 10
